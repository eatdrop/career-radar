# Changelog

## 1.1.0 - 2026-07-29

- 雷达执行升级为 SQLite 持久任务：异步提交、幂等去重、状态轮询、刷新续跑与进程重启恢复。
- 增加 claim token、可续租 lease、旧 Worker fencing、指数退避和失败次数上限。
- 邮件改用事务 Outbox；雷达结果与邮件意图原子落库，并隔离 SMTP 结果不明确的发送，避免盲目重复。
- 过期邮件发送租约改为人工对账隔离，增加 `jobsearch-ops` 确认送达与受控重试命令。
- 最终任务尝试增加一次有界业务对账；雷达锁竞争改为不消耗重试预算的 defer。
- 数据库增加显式 schema v3 迁移，兼容无版本旧库、v1 与 v2，并拒绝由更高版本创建的数据库。
- 定时器改为按时区和自然日写入哈希幂等任务，兼容所有合法 IANA 时区。
- 前端增加排队/运行/重试状态、三分钟渐进轮询、429 退避和 `sessionStorage` 断点恢复。
- 增加严格 Host allowlist 与重复 Host 拒绝，阻断本地无认证服务的 DNS-rebinding 入口。
- 异步错误改为稳定结构化 DTO，列表接口与邮件 Outbox 按用途最小化敏感数据。
- 隐私模式递归脱敏结构化内容中的邮箱、电话、即时通讯和常见公开账号。
- CI 增加最终 wheel 的全新虚拟环境安装、依赖一致性与 `/readyz` 启动验证。
- 增加非 root Docker 镜像构建与容器就绪验证，构建流程改为多阶段镜像。
- 增加 Dependabot、漏洞披露策略和正式发布检查清单。
- GitHub 官方 Actions 固定到已签名版本的完整提交 SHA。
- 自动化测试增至 84 项，覆盖队列并发领取、租约续期、崩溃对账、Outbox、Host 边界和异步 API。

## 1.0.0 - 2026-07-29

- 将项目重构为每日求职雷达，打通简历画像、岗位过滤、证据匹配、定向优化与投递看板。
- 增加本地脱敏证据检索、单次 AI 授权、请求限流、安全响应头和隐私安全默认值。
- 增加每日调度、SerpAPI、51job、SMTP、Docker、MCP 与跨平台 CI 支持。
- 增加 35 项自动化测试、真实浏览器验收和 wheel/sdist 发布产物。
