# 智职引擎：基于 RAG 与大模型的大学生全栈求职智能助手

## 作品简介

智职引擎是一套面向大学生的“每日求职雷达”。它关注的不是单次生成一份简历，而是解决求职过程中最消耗时间的重复工作：每天跨平台搜索、排除资深和社招岗位、逐条对照 JD、反复调整简历，以及投递后缺少持续跟踪。

用户只需维护一份简历画像和一组求职偏好，系统即可按计划聚合岗位，先用城市、年限、Senior/Lead、外包等硬规则过滤噪声，再检索简历中与岗位最相关的真实证据，对候选岗位进行可解释排序。每个推荐岗位都会展示匹配原因、引用的简历证据、能力缺口和投递前修改动作，结果可保存在站内，也可通过邮件按日发送，并能一键进入投递看板。

## 核心工作流

1. **设置求职偏好**：岗位方向、目标城市、校招优先、排除词、最低匹配分；
2. **建立简历画像**：解析 TXT、DOCX、PDF 或粘贴文本，形成结构化能力与证据块；
3. **每日岗位聚合**：支持本地岗位池、Google Jobs 聚合和 51job 可选采集；
4. **硬条件过滤**：排除资深岗位、过高年限、外包和自定义噪声；
5. **RAG 证据匹配**：为每个 JD 检索相关简历片段，生成可追溯的匹配理由；
6. **逐岗简历动作**：指出应前置的项目、应补充的真实指标和待核实关键词；
7. **摘要与投递闭环**：站内或邮件接收每日结果，一键建档并跟踪投递状态。

## 技术特色

- Python 标准库即可运行核心 Web 服务，AI、MCP、爬虫和 PDF 能力按需安装；
- REST 与 MCP 共用同一业务服务，避免前后端接口漂移；
- 本地 SQLite 保存岗位池、简历证据、雷达运行和投递记录；
- RAG 使用真实的简历证据检索，不在 Embedding 失败时伪造向量；
- DeepSeek 可选增强只有在用户对本次处理明确授权后才会调用，并受证据约束，禁止编造经历、技能和数字；
- 默认不持久化原始简历与联系方式，只保留可用于匹配的脱敏证据；
- 定时任务按自然日幂等，外部来源失败会返回明确状态；
- 默认本机监听、关闭跨域，并具备限流、并发上限、安全响应头和请求追踪；
- 前端提供完整的响应式工作台，而不是相互割裂的演示页面。

## 创新价值

相较于“输入 JD—生成文本”的单点工具，智职引擎把 RAG 与大模型放进一个持续运行的求职业务流程中。系统先解决岗位信息过载，再解决人与岗位的证据匹配，最后把推荐转化为可执行的投递动作。这种设计更贴近大学生每天真实发生的求职场景，也能清晰衡量系统价值：减少无效岗位阅读量、缩短筛选时间、提高定向投递质量，并保留全过程数据。
